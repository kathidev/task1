pipeline
{

    agent any

    tools {
           
	  maven 'MAVEN_HOME'

          jdk 'JAVA_HOME'

	}
    stages
	{

         stage('chekoutstage')
      {
         steps
         {
            
            checkout scm
         }
      }
       stage('ContinuousBuild') 
      {
       steps
       {
        sh 'mvn package'
      } 
    }
 }	
}
